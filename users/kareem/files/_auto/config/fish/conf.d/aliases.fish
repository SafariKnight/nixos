# Better defaults
# abbr ls "eza --group-directories-first --icons" # doesn't work yippie
abbr cat bat
abbr grep rg
abbr find fd
abbr less "less -r"

abbr pn pnpm
abbr npm pnpm

# Nix
alias nrepl="nix repl -f flake:nixpkgs --extra-experimental-features pipe-operators"
